import Query from "./Query";

export default class OrderQuery extends Query{

    public readonly ORDER_TYPE_FILTER = "order_type"
    public readonly SERVICE_FILTER = "service"
    public readonly DATE_FILTER = "order_at"


}

